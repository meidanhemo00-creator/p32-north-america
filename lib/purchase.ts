// Single source of truth for every "Purchase the book" call to action on the
// site. Replace this placeholder once a real storefront link exists — every
// button reads from here, so one edit updates the whole page.
export const PURCHASE_URL = "ADD_PURCHASE_LINK_HERE";
